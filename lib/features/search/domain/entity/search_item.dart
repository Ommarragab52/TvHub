class SearchItem {
  final int id;
  final String title;
  final String? posterPath;
  final String mediaType;

  const SearchItem({
    required this.id,
    required this.title,
    required this.posterPath,
    required this.mediaType,
  });
}
